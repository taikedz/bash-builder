# Todo

* add support in installer to optionally clone and install standard libraries
* change `bbuild` argument logic: `bbuild SOURCE DEST`
* change `bbuildrc` to a sequence of custom commands

    # bbuildrc file

    shellcheck on
    addpath PATH1 PATH2

    buildto ./bin src/main.sh src/supporting.sh

    buildto ./ src/install.sh
